/*
 date:
 author: dp
 */
 -- de
 select 789 ;

-- set var: a = hello;
select now(), '${a}', '-- de';
--  select 123;

-- select
--     id,
--     name ,
--     first_value(name) over(partition by id order by  case when name is null then null else date end nulls last ) as name2 ,
--     date
-- from paimon.test.test_empi_merge;

